<!DOCTYPE html>
<html class="outer-div">
  <head>
    <title>Mytwitterrailsemberapp</title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div>
      <% if current_user %>
        You are signed in as <%= current_user.name %>!
        <button><%= link_to "Sign Out", signout_path %></button>
        <button><%= link_to "See Your Tweets", "http://localhost:3000/tweets"%></button>
      <% else %>
        <button><%= link_to "Sign in with Twitter", "/auth/twitter" %></button>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
